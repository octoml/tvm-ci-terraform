name: terraform
on:
  pull_request:
    branches: [main]

jobs:
  plan:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v2
      - name: Terraform fmt
        id: fmt
        uses: dflook/terraform-fmt@eaf38cf4b378f2b015a1ad7e17570de0b788e69b
        continue-on-error: true
      - name: Terraform Validate
        id: validate
        uses: dflook/terraform-validate@eaf38cf4b378f2b015a1ad7e17570de0b788e69b
      - name: Terraform Plan
        uses: dflook/terraform-plan@eaf38cf4b378f2b015a1ad7e17570de0b788e69b
        id: plan
        with:
          var_file: |
            vars/tvm-ci-prod.auto.tfvars
          workspace: tvm-ci-prod
